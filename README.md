# Chat With Your Map
This is the codebase for the backend

# Start
To start the development you have to first create an conda environment with
```bash
conda env create -f environment.yml -p .venv/
```

And then install all dependencies with
```bash
poetry install
```

And the you are good to go...